# Supabase delta handoff

Use the supplied 34-table schema as the baseline. Run `supabase/schema-preflight.sql` (read-only) to inspect types, enums, defaults, constraints, triggers, RLS, buckets and realtime. Reconcile and apply the pending `202609140001`–`202609140016` migration batch in staging, in order, without app traffic between dependent scripts. Earlier pending files were corrected; do not replay them blindly if an older version was already applied. Do not regenerate the existing schema.

1. New tables: `admin_members`, `saved_jobs`, `listing_reports`, `listing_moderation`, `admin_audit_log`, `verification_requests`, `project_submissions`, `revision_requests`, `account_moderation`, `project_disputes`, `project_cancellations`, `planner_documents`, `project_meetings`.
2. Reuse existing `saved_services`, `contract_item_approvals` (including timestamps/unique key), contracts, projects, milestones, reviews and payments. Preserve `service_milestones`/`contract_milestones`; no payment schema changes.
3. Orders: nullable `service_id`; add `job_id`/`application_id` FKs and unique application/order. Inspect existing rows before validating the single-source constraint. Preserve existing contract/order and favorite uniqueness.
4. Reconcile enum/check labels: orders pending/accepted/rejected/active/completed/cancelled; contracts draft/active/completed/cancelled; projects/milestones pending/active/completed/cancelled; services Active; jobs open; applications pending/in_review/accepted/rejected; verification verified/rejected; review roles client/freelancer. Reconcile notification types from the scripts. Do not guess enum names or recreate types.
5. Install checked workflow, analytics, moderation, resolution and planner/meeting RPCs and triggers. Preserve transaction locks, participant/admin authorization, atomic status synchronization and audit records. Inspect legacy signed contracts and existing notification/activation triggers to avoid duplicate effects.
6. RLS/grants: own favorites/planner; participant work, meetings and resolution; admin moderation/analytics. Restrict new business for suspended users while allowing existing work/messages/resolution. Private dispute context access is scoped and audited. Use trusted `admin_members`; prohibit self-enrollment.
7. Private buckets `project-attachments` and `verification-documents`, 10 MB. Reconcile all broad policies: authorized reads, owner-path uploads, immutable linked files, orphan-only cleanup, no anonymous access. Admin project-file reads require a related dispute. Retain messaging policies and separately reconcile pending `202609060001` messaging migration.
8. Realtime: contracts, approvals, projects, milestones, submissions, revisions, reviews, notifications, orders, account moderation, disputes, cancellations, planner documents and meetings. Preserve messaging publication.
9. Keep the provided FK/index/uniqueness guards. Validate source identity, one open resolution request, cross-project access and storage ownership. No Gemini, PayMongo, refunds or payment releases. Existing Supabase URL/anon-key environment only.

Test in staging with client, freelancer, unrelated user and admin: signing races, suspended actions/expiry, cancellations, disputes/evidence, revisions/completion/reviews, private file denial, planner conflicts and meeting/deadline refresh. These scripts have not been executed against the live database.
